# screeps2
Install visual code

Install NPM

go into directory and run npm install

create secrets.js that looks like
```
module.exports = {
    email: 'username',
    password: 'password'
};
```

run `npm start` to monitor code changes and upload to account

script being uploaded can be found in dist\main.js